services:
  - type: web
    name: moviestreamhub
    env: node
    buildCommand: ./build.sh
    startCommand: node dist/server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
